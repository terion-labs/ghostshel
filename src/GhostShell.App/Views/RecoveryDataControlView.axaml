<UserControl
    x:Class="GhostShell.App.Views.RecoveryDataControlView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:GhostShell.App.Controls"
    xmlns:icons="using:FluentIcons.Avalonia">
  <controls:SurfaceCard Padding="0">
    <StackPanel Margin="{controls:Inset Lg}" Spacing="{DynamicResource ShellSpaceMd}">
      <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="{DynamicResource ShellSpaceSm}">
        <StackPanel Spacing="{DynamicResource ShellSpaceXs}">
          <TextBlock FontWeight="SemiBold" Text="Crash recovery data" />
          <TextBlock
              Classes="Muted"
              FontSize="{DynamicResource ShellFontSize10}"
              Text="Review bounded metadata for snapshots left by previous runs."
              TextWrapping="Wrap" />
        </StackPanel>
        <Button
            x:Name="RefreshRecoveryDataButton"
            Grid.Column="1"
            Classes="SecondaryButton"
            Click="OnRefreshClick"
            Content="Refresh"
            IsEnabled="{Binding CanRefresh}"
            AutomationProperties.Name="Refresh crash recovery data" />
        <Button
            Grid.Column="2"
            Classes="DestructiveButton"
            Click="OnClearAllClick"
            Content="Clear all previous"
            IsEnabled="{Binding CanClearAll}"
            AutomationProperties.Name="Clear all previous crash recovery data" />
      </Grid>

      <controls:SurfaceCard Tone="Notice" Padding="0">
        <Grid Margin="{controls:Inset Md}" ColumnDefinitions="Auto,*" ColumnSpacing="{DynamicResource ShellSpaceSm}">
          <icons:SymbolIcon Symbol="Shield" FontSize="16" />
          <TextBlock
              Grid.Column="1"
              Classes="Muted"
              Text="Only previous-run metadata is listed. The current run’s crash snapshot is hidden and protected. Snapshot payloads are never loaded into this screen."
              TextWrapping="Wrap" />
        </Grid>
      </controls:SurfaceCard>

      <TextBlock
          Classes="Muted Monospace"
          FontSize="{DynamicResource ShellFontSize9}"
          Text="{Binding StatusMessage}"
          TextWrapping="Wrap"
          AutomationProperties.LiveSetting="Polite"
          AutomationProperties.Name="{Binding StatusAutomationName}" />

      <controls:SurfaceCard Padding="0" IsVisible="{Binding HasNoRuns}">
        <controls:EmptyStatePanel Margin="{controls:Inset Md}"
            ShowsGlyph="False"
            Heading="No previous recovery data"
            Body="The protected snapshot for the current run is intentionally not shown." />
      </controls:SurfaceCard>

      <controls:SurfaceCard Tone="Notice" Padding="0" IsVisible="{Binding HasError}">
        <StackPanel Margin="{controls:Inset Md}" Spacing="{DynamicResource ShellSpaceXs}">
          <TextBlock FontWeight="SemiBold" Text="Recovery metadata unavailable" />
          <TextBlock
              Classes="Muted"
              Text="The current recovery inventory could not be loaded. No further data will be removed automatically; use Refresh to try again."
              TextWrapping="Wrap" />
        </StackPanel>
      </controls:SurfaceCard>

      <ItemsControl
          ItemsSource="{Binding Runs}"
          IsEnabled="{Binding CanClearRuns}"
          IsVisible="{Binding HasRuns}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <controls:SurfaceCard Tone="Notice" Padding="0" Margin="{controls:Inset Bottom=Sm}">
              <Grid Margin="{controls:Inset Md}" ColumnDefinitions="*,Auto" ColumnSpacing="{DynamicResource ShellSpaceMd}">
                <StackPanel Spacing="{DynamicResource ShellSpaceXs}">
                  <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                  <WrapPanel Orientation="Horizontal">
                    <TextBlock Margin="{controls:Inset Right=Md}" Classes="Monospace" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding SnapshotLabel}" />
                    <TextBlock Margin="{controls:Inset Right=Md}" Classes="Monospace Muted" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding SizeLabel}" />
                    <TextBlock Classes="Muted" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding UpdatedLabel}" />
                  </WrapPanel>
                </StackPanel>
                <Button
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Classes="DestructiveButton"
                    Click="OnClearRunClick"
                    Content="Clear"
                    AutomationProperties.Name="{Binding ClearAutomationName}" />
              </Grid>
            </controls:SurfaceCard>          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </controls:SurfaceCard>
</UserControl>
