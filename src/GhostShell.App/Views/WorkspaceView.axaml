<UserControl
    x:Class="GhostShell.App.Views.WorkspaceView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:GhostShell.App.Controls"
    xmlns:icons="using:FluentIcons.Avalonia"
    xmlns:views="using:GhostShell.App.Views"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch">
  <Grid RowDefinitions="44,34,*,26">
    <Border
        Classes="TopChrome"
        PointerPressed="OnTitleBarPointerPressed"
        WindowDecorationProperties.ElementRole="TitleBar">
      <Grid ColumnDefinitions="Auto,*,Auto" Margin="{Binding WindowTitleBarContentMargin}" ColumnSpacing="8">
        <Button
            x:Name="WorkspaceLauncherButton"
            Classes="IconButton"
            Click="OnShowLauncherClick"
            ToolTip.Tip="Launcher"
            WindowDecorationProperties.ElementRole="User"
            AutomationProperties.Name="Open launcher">
          <icons:SymbolIcon Symbol="Home" FontSize="15" />
        </Button>
        <ScrollViewer
            x:Name="RuntimeTabStrip"
            Grid.Column="1"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Disabled"
            WindowDecorationProperties.ElementRole="User"
            AutomationProperties.Name="Open runtime tabs">
          <ItemsControl ItemsSource="{Binding RuntimeWorkspace.Tabs}">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="5" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Grid
                    Width="190"
                    Height="32"
                    Classes="RuntimeTabDropTarget"
                    ColumnDefinitions="24,*,28"
                    DragDrop.AllowDrop="True"
                    DragDrop.DragEnter="OnRuntimeTabDragEnter"
                    DragDrop.DragLeave="OnRuntimeTabDragLeave"
                    DragDrop.DragOver="OnRuntimeTabDragOver"
                    DragDrop.Drop="OnRuntimeTabDrop">
                  <Button
                      Grid.ColumnSpan="3"
                      Padding="32,0,32,0"
                      Background="{Binding TabBackground}"
                      BorderThickness="0"
                      Classes="RuntimeTabActivator"
                      Click="OnActivateTabClick"
                      HorizontalContentAlignment="Left"
                      AutomationProperties.Name="{Binding Title, StringFormat={}Activate tab {0}}"
                      AutomationProperties.HelpText="Use the reorder handle to drag this tab, or use Move tab left and Move tab right from the command palette.">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                      <icons:SymbolIcon Symbol="WindowConsole" FontSize="13" Foreground="{Binding TabForeground}" />
                      <TextBlock Foreground="{Binding TabForeground}" FontSize="{DynamicResource ShellFontSize11}" Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </Button>
                  <Border
                      Grid.ColumnSpan="3"
                      Width="3"
                      Margin="0,3"
                      HorizontalAlignment="Left"
                      Background="{DynamicResource ShellAccentBrush}"
                      Classes="RuntimeTabDropIndicator Before"
                      IsHitTestVisible="False"
                      IsVisible="False" />
                  <Border
                      Grid.ColumnSpan="3"
                      Width="3"
                      Margin="0,3"
                      HorizontalAlignment="Right"
                      Background="{DynamicResource ShellAccentBrush}"
                      Classes="RuntimeTabDropIndicator After"
                      IsHitTestVisible="False"
                      IsVisible="False" />
                  <Border
                      Grid.Column="0"
                      Background="Transparent"
                      PointerCaptureLost="OnRuntimeTabDragPointerCaptureLost"
                      PointerMoved="OnRuntimeTabDragPointerMoved"
                      PointerPressed="OnRuntimeTabDragPointerPressed"
                      PointerReleased="OnRuntimeTabDragPointerReleased"
                      ToolTip.Tip="Drag to reorder tab"
                      AutomationProperties.Name="{Binding Title, StringFormat={}Reorder tab {0}}"
                      AutomationProperties.HelpText="Drag left or right to change this tab's position without activating it.">
                    <icons:SymbolIcon
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Symbol="ReOrderDotsVertical"
                        FontSize="12"
                        Foreground="{Binding TabForeground}" />
                  </Border>
                  <Button
                      Grid.Column="2"
                      Classes="IconButton"
                      Click="OnCloseRuntimeTabClick"
                      ToolTip.Tip="Close tab"
                      AutomationProperties.Name="{Binding Title, StringFormat={}Close tab {0}}"
                      AutomationProperties.HelpText="Close this runtime tab using the normal session close policy.">
                    <icons:SymbolIcon Symbol="Dismiss" FontSize="11" />
                  </Button>
                </Grid>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
        <StackPanel
            x:Name="WorkspaceTitleBarActions"
            Grid.Column="2"
            Orientation="Horizontal"
            VerticalAlignment="Center"
            Spacing="5"
            WindowDecorationProperties.ElementRole="User">
          <Button Classes="IconButton" Click="OnShowNewPanelClick" ToolTip.Tip="Add panel" AutomationProperties.Name="Add panel"><icons:SymbolIcon Symbol="Add" FontSize="15" /></Button>
          <Button Classes="IconButton" Click="OnToggleAgentClick" ToolTip.Tip="Toggle agent panel" AutomationProperties.Name="Toggle AI agent panel"><icons:SymbolIcon Symbol="Bot" FontSize="15" /></Button>
          <Button Classes="IconButton" Click="OnShowSettingsClick" ToolTip.Tip="Settings" AutomationProperties.Name="Open settings"><icons:SymbolIcon Symbol="Settings" FontSize="15" /></Button>
        </StackPanel>
      </Grid>
    </Border>

    <Border Grid.Row="1" Background="{DynamicResource ShellSurfaceRaisedBrush}" BorderBrush="{DynamicResource ShellBorderBrush}" BorderThickness="0,0,0,1">
      <Grid ColumnDefinitions="Auto,*,Auto" Margin="11,0" ColumnSpacing="10">
        <TextBlock VerticalAlignment="Center" Classes="Eyebrow Accent" Text="{Binding RuntimeWorkspace.Name}" />
        <ItemsControl Grid.Column="1" ItemsSource="{Binding RuntimeWorkspace.Connections}">
          <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="7" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Padding="8,3" Background="{DynamicResource ShellSurfaceHoverBrush}" CornerRadius="6">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <Ellipse Width="6" Height="6" VerticalAlignment="Center" Fill="#57B879" />
                  <TextBlock Classes="Monospace" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding Name}" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Grid.Column="2" Classes="TextButton" Click="OnShowCommandPaletteClick" Content="{Binding CommandPaletteAction}" />
      </Grid>
    </Border>

    <DockPanel Grid.Row="2" LastChildFill="True">
      <Border DockPanel.Dock="Left" Width="56" Background="{DynamicResource ShellSurfaceBrush}" BorderBrush="{DynamicResource ShellBorderBrush}" BorderThickness="0,0,1,0">
        <Grid RowDefinitions="*,Auto" Margin="8,9">
          <ItemsControl ItemsSource="{Binding Workspaces}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Width="40" Height="40" Margin="0,0,0,7" Padding="0" Background="{Binding Accent}" Click="OnOpenWorkspaceClick" ToolTip.Tip="{Binding Name}" AutomationProperties.Name="{Binding Name}">
                  <icons:SymbolIcon Symbol="{Binding IconSymbol}" FontSize="16" />
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Button Grid.Row="1" Width="40" Height="40" Padding="0" Click="OnShowNewItemClick" ToolTip.Tip="New tab" AutomationProperties.Name="Create a new tab"><icons:SymbolIcon Symbol="Add" FontSize="14" /></Button>
        </Grid>
      </Border>

      <views:AgentWorkspaceView
          x:Name="AgentWorkspaceSurface"
          DockPanel.Dock="Right"
          Width="352"
          Margin="0,8,8,8"
          AgentQuestionResponseKeyDownRequested="OnAgentQuestionResponseKeyDown"
          ApproveAgentActionRequested="OnApproveAgentActionClick"
          CancelAgentActionRequested="OnCancelAgentActionClick"
          CancelAgentChatRequested="OnCancelAgentChatClick"
          ClearAgentChatRequested="OnClearAgentChatClick"
          DeclineAgentQuestionRequested="OnDeclineAgentQuestionClick"
          DenyAgentActionRequested="OnDenyAgentActionClick"
          DisableAgentYoloRequested="OnDisableAgentYoloClick"
          EnableAgentCapabilityAskRequested="OnEnableAgentCapabilityAskClick"
          EnableAgentYoloRequested="OnEnableAgentYoloClick"
          IsVisible="{Binding IsAgentPanelVisible}"
          KeepAgentCapabilityOffRequested="OnKeepAgentCapabilityOffClick"
          LoadOlderAgentAuditRequested="OnLoadOlderAgentAuditClick"
          RefreshAgentAuditRequested="OnRefreshAgentAuditClick"
          SendAgentChatRequested="OnSendAgentChatClick"
          ShowAgentSettingsRequested="OnShowAgentSettingsClick"
          SubmitAgentQuestionRequested="OnSubmitAgentQuestionClick" />

      <Grid Margin="8" IsVisible="{Binding IsWorkspaceCanvasVisible}">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
          <ItemsControl
              MinWidth="{Binding RuntimeWorkspace.ActiveTab.MinimumCanvasWidth}"
              MinHeight="{Binding RuntimeWorkspace.ActiveTab.MinimumCanvasHeight}"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              ItemsSource="{Binding RuntimeWorkspace.ActiveTab.Panels}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <controls:RuntimePanelLayoutPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Styles>
              <Style Selector="ItemsControl > ContentPresenter"><Setter Property="Margin" Value="0,0,8,8" /></Style>
            </ItemsControl.Styles>
          </ItemsControl>
        </ScrollViewer>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10" IsVisible="{Binding !HasRuntimeWorkspace}">
          <TextBlock Classes="Muted" Text="Open a workspace from the launcher." />
          <Button Classes="PrimaryButton" Click="OnShowLauncherClick" Content="Open launcher" />
        </StackPanel>
      </Grid>
    </DockPanel>

    <Border Grid.Row="3" Background="{DynamicResource ShellSurfaceBrush}" BorderBrush="{DynamicResource ShellBorderBrush}" BorderThickness="0,1,0,0">
      <Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto" ColumnSpacing="14" Margin="10,0">
        <TextBlock VerticalAlignment="Center" Classes="Muted Monospace" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding RuntimeWorkspace.ActiveTab.Source}" />
        <TextBlock Grid.Column="1" VerticalAlignment="Center" Classes="Muted" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding WorkspaceStatus}" />
        <TextBlock
            Grid.Column="2"
            VerticalAlignment="Center"
            Classes="Muted"
            FontSize="{DynamicResource ShellFontSize9}"
            Text="{Binding TabReorderStatus}"
            TextTrimming="CharacterEllipsis"
            AutomationProperties.LiveSetting="Polite"
            AutomationProperties.Name="{Binding TabReorderStatus, StringFormat={}Tab order status · {0}}" />
        <TextBlock Grid.Column="3" VerticalAlignment="Center" Classes="Muted" FontSize="{DynamicResource ShellFontSize9}" Text="{Binding HostStatus}" />
        <TextBlock
            Grid.Column="4"
            VerticalAlignment="Center"
            Classes="Eyebrow"
            Text="{Binding AgentChat.ConnectionStatus, StringFormat={}AGENT · {0}}" />
      </Grid>
    </Border>
  </Grid>
</UserControl>
